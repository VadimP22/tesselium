use crate::Window;

pub struct InitializationHandler {}

impl InitializationHandler {
    pub(crate) fn create(window: &mut Window) -> Self {
        todo!()
    }
    ///Load texture. Returns texture handler.
    pub fn load_texture(&mut self) {
        todo!()
    }
}

impl Drop for InitializationHandler {
    fn drop(&mut self) {
        todo!()
    }
}
